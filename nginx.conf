server {
    listen 3000;  # Porta 3000 (match com EXPOSE do Dockerfile)
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Adicionar tipos MIME corretos
    types {
        text/html html htm;
        text/css css;
        application/javascript js;
        application/json json;
        image/png png;
        image/jpeg jpg jpeg;
        image/svg+xml svg svgz;
        font/woff2 woff2;
        font/woff woff;
    }
    default_type application/octet-stream;

    # Servir assets reais do Chatbot Teacher (com cache e MIME type correto)
    location /teacher/assets/ {
        alias /usr/share/nginx/html/assets/;
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
        
        # MIME types explícitos
        location ~* \.(js)$ {
            add_header Content-Type "application/javascript; charset=utf-8";
        }
        location ~* \.(css)$ {
            add_header Content-Type "text/css; charset=utf-8";
        }
    }

    # Servir assets para base path "/" (com cache e MIME type correto)
    location /assets/ {
        alias /usr/share/nginx/html/assets/;
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
        
        # MIME types explícitos
        location ~* \.(js)$ {
            add_header Content-Type "application/javascript; charset=utf-8";
        }
        location ~* \.(css)$ {
            add_header Content-Type "text/css; charset=utf-8";
        }
    }

    # SPA em /teacher/
    location /teacher/ {
        alias /usr/share/nginx/html/;
        try_files $uri $uri/ /usr/share/nginx/html/index.html;
    }

    # Fallback para root (caso acesse diretamente)
    location / {
        try_files $uri $uri/ /index.html;
        add_header Content-Type "text/html; charset=utf-8";
    }

    # Configuração para health check
    location /health {
        access_log off;
        add_header Content-Type text/plain;
        return 200 "healthy\n";
    }

    # Configuração de segurança
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;
    add_header Content-Security-Policy "default-src 'self' http: https: data: blob: 'unsafe-inline'" always;
}